Item {
    property pressed: false
    property source: pressed ? "pressed.img" : "unpressed.img"
    property padding: 0
    property text: ""

    id: btn
    x: padding
    y: padding
    width: parent.width - padding * 2
    height: parent.height - padding * 2
}